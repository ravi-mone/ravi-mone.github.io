/**
 * Created by Ravi on 3/26/14.
 */
'use strict';

angAuth.value('log', window.console.log);

/*
 * Module.value(key, value) is used to inject an editable value,
 * Module.constant(key, value) is used to inject a constant value */

